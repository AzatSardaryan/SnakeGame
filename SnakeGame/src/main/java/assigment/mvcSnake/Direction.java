package assigment.mvcSnake;

public enum Direction {
	
	// simple enum class to help with direction control and movement
	// when the user presses an arrow key it will set the Direction to the given enum
		UP, 
		DOWN,
		LEFT, 
		RIGHT;
}
